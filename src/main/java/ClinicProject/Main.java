package ClinicProject;

import ClinicProject.Sorting.SortingType;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
        // Ceci n'est pas un test!! C'est un exemple d'utilisation.
        SortingType doctorSortingType = SortingType.FIFO;
        SortingType radiologySortingType = SortingType.FIFO;

        // Clinic clinic = new Clinic(doctorSortingType, radiologySortingType /* , ... */)
        // clinic.triagePatient("John", 4, VisibleSymptom.MIGRAINE);
    }
}